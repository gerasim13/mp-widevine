pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'docker-compose build cdn'
            }
        }
        stage('Run') {
            steps {
                sh 'docker run -d -p 6060:6060 tvup-cdn /usr/sbin/apache2ctl -D FOREGROUND'
            }
        }
        stage('Deploy') {
            steps {
                echo 'DEPLOYy'
            }
        }
    }
}